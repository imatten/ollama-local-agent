[project]
name = "ollama-local-agent"
version = "0.1.0"
description = "Learning playground for local LLM agents"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.4.2",
    "langchain-ollama>=1.1.0",
    "requests",
]
